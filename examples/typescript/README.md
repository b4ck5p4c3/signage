# signage-typescript-example

Simple example about how to send messages to signage using TypeScript

To run:
```shell
yarn
yarn main
```

The protocol itself is in `signage/` directory, copy and use it as you wish